spring:
  application:
    name: auth

  datasource:
    url: jdbc:postgresql://localhost:5432/authdb
    username: postgres
    password: ${psql_password}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

jwt:
  secret: ${JWT_SECRET}
